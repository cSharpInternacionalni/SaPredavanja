<Query Kind="Statements" />

int x = 5;

if (x > 5 )
	Console.WriteLine("Vece je!");
else if (x == 5)
	Console.WriteLine("Isto je!");
else
	Console.WriteLine("Manje je!");
	
switch(x)
{
	case 16:
		Console.WriteLine("Slucaj 16");
		break;
	case 5:
		Console.WriteLine("Isto je!");
	break;
	
	default:
	break;
}


if (50 < 6)
{
	Console.WriteLine("Manje je!!");
	Console.WriteLine("Treba jos nesto!");
}